# Pre-Submission Audit — Every Remaining Placeholder

Generated by grepping `<<...>>` across the actual submission files (not reconstructed from memory). Nothing below is a template note — these are the literal blockers standing between the current package and a submittable one. Organize your fill-in pass by file, in this order, since `titlepage.tex`'s author list is the one piece of information the other files derive from.

---

## 1. `titlepage.tex` — must fill before compiling the camera-ready title page

| Line | Placeholder | What's needed |
|---|---|---|
| 13 | `<<AUTHOR ONE FULL NAME>>` | First (corresponding) author's full name |
| 14 | `<<AUTHOR ONE EMAIL>>` | Corresponding author's email |
| 16 | `<<AUTHOR TWO FULL NAME>>` | Second author's full name — **delete this `\author` block entirely if single-author** |
| 17 | `<<AUTHOR TWO EMAIL>>` | Second author's email (if kept) |
| 19 | `<<DEPARTMENT, INSTITUTION, CITY, COUNTRY>>` | Affiliation string for `inst1` |
| 20 | `<<SECOND AFFILIATION, IF ANY>>` | Affiliation for `inst2` — **delete if not needed** |
| 23, 24 | `<<0000-0000-0000-0000>>` | ORCID iDs (optional at some journals, increasingly required — check BSPC's current policy) |

**Action if single-author**: delete the entire second `\author[inst1,inst2]{...}` block and its `\ead`, and the `inst2` address line and its `fntext`.

## 2. `cover_letter.tex` — must fill before sending

| Line | Placeholder | What's needed |
|---|---|---|
| 8 | `<<CORRESPONDING AUTHOR FULL NAME>>` | Same person as `titlepage.tex`'s corresponding author |
| 9 | `<<DEPARTMENT, INSTITUTION>>`, `<<CITY, COUNTRY>>`, `<<EMAIL>>` | Same affiliation/email as title page |

Body text is final and was not templated — no other edits needed unless the science summary itself changes.

## 3. `declarations.tex` — three categories

**(a) Fill only if the "no X" assumption is wrong** — leave as-is otherwise:
- Line 25: competing-interest statement — replace only if an author *has* a conflict.
- Line 31: funding statement — replace only if this work *was* funded.
- Line 37: ethics-exemption note — add only if an author's institution specifically requires a formal statement/IRB reference for secondary analysis of public data (many don't).

**(b) Must fill regardless**:
- Line 44: `<<verify exact DOI before submission>>` — PAD-UFES-20's Mendeley Data DOI. Not fabricated here because exact DOI digits weren't independently verified; look it up directly rather than trusting a recalled number in a submission.
- Line 43: verify the exact current ISIC Archive access URL for the 2018 challenge data (ISIC's site structure has changed over time).
- Line 47: `<<CODE/DATA REPOSITORY URL, IF MADE PUBLIC>>` — a genuine decision, not just a fill-in: do you want to publish the analysis code/results? If yes, provide the URL; if no, this line should say so explicitly ("Code and analysis scripts are available from the corresponding author upon reasonable request") rather than being left blank.

**(c) Must fill, one per author**:
- Lines 58–70: CRediT role initials. Assign per author; delete roles that don't apply (e.g., "Funding acquisition" if there was none).

## 4. Cross-file consistency check (do this last, after 1–3 are filled)

- Corresponding author's name/email/affiliation must be **identical** across `titlepage.tex`, `cover_letter.tex`, and (implicitly) wherever BSPC's submission portal asks for it again.
- If multi-author, decide author order now — it must match across `titlepage.tex` and any portal entry.

---

## Not a placeholder, but flagged elsewhere — don't lose these

From `submission_checklist.md`, the two items that are genuine open decisions rather than fill-in-the-blank:
1. Whether to make code/results public (declarations.tex line 47, above).
2. Whether any author has an ORCID they want listed (optional but increasingly expected).

Everything else in the package (manuscript body, all 4 sections, all figures/tables, abstract, highlights, all 5 non-conditional declaration statements) is final content, not a placeholder — it does not need author review for factual accuracy of the science, only for the items listed above.
